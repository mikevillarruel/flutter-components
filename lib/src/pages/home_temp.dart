import 'package:flutter/material.dart';

class HomePagaTemp extends StatelessWidget {
  const HomePagaTemp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Home Temp'),
    );
  }
}